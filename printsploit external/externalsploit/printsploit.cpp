#include "Init.h"
#include <filesystem>

int main() {
	execute_function(test_print);
}

// this is external printsploit [NO CRASH]
// u can also use this to inject functions and hook it to your bridge and call it [getrawmetatable, etc]
// made by nbeater678, and yes you can skid this
// credits: the guy who made the Utils, thanks.
// set as release not debug!